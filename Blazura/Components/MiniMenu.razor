@using static Blazura.Components.IconButton

<IconButton Type="@Type" Icon="@Icon" @onclick="(e) => contextMenu.Open(e)" />
<ContextMenu @ref="contextMenu">
    @ChildContent
</ContextMenu>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public string Icon { get; set; } = "more_vert";
    [Parameter] public IconButtonType Type { get; set; }

    private ContextMenu contextMenu = default!;
}
